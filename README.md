# Basic NodeJS calculator using yargs

Usage:
```
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Multiplying base                            [number] [required]
  -l, --list     Show result on consule                                [boolean]
  -u, --until    Multiply until                                         [number]
```